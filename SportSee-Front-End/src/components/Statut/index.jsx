/* import { Link } from 'react-router-dom';

export default function Card({ title, cover, href }) {
  return (
    <Link to={`logement/${href}`} className="cards">
      <img className="cards__img"  src={cover} alt={'Photo of ' + title} />
      <span className="cards__title">{title}</span>
    </Link>
  );
}
 */